export class Card {
  constructor(
    public name: string,
    public copes: number,
  ) {};
}

// Clase que representa un objeto que vamos a manejar.
// En este caso es una carta con nombre y sus copias.